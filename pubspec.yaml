name: ai_calorie_counter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.3
  fl_chart: ^1.1.1
  intl: ^0.20.2
  sqflite: ^2.4.2
  path: ^1.9.1
  http: ^1.6.0
  google_mobile_ads: ^7.0.0
  flutter_dotenv: ^6.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
