import 'package:flutter/material.dart';

class WaterTrackerGoalScreen extends StatelessWidget {
  const WaterTrackerGoalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Water Tracker')));
  }
}
